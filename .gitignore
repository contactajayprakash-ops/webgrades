node_modules
dist
dist-ssr
*.local
.env
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Firebase CLI
.firebase/
.trash-firebase-init/

# Pi-only secrets — service account key + credential stores. NEVER commit these.
*serviceaccount*.json
*service-account*.json
firebase-sa*.json
snapshot-subs.json
push-subs.json

# Firebase "agent skills" - docs the CLI drops in, not part of this project
.agents/
.claude/skills/
skills-lock.json
